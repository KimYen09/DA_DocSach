<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingRight="10dp"
    android:paddingLeft="10dp"
    tools:context=".adminactivity.WriteActivity">

    <!-- Nội dung chính được sắp xếp theo chiều dọc -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Nút quay lại (nếu cần) -->
        <ImageView
            android:id="@+id/backforget"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginTop="20dp"
            android:src="@drawable/baseline_arrow_back_24"
            android:contentDescription="Quay lại"
            android:layout_marginBottom="10dp"/>

        <!-- Tiêu đề -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Quản lý truyện"
            android:fontFamily="@font/font_lexend"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp"
            android:layout_gravity="center_horizontal"/>

        <!-- Thanh tìm kiếm -->
        <androidx.appcompat.widget.SearchView
            android:id="@+id/searchViewStories"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:queryHint="Tìm kiếm truyện..."
            android:fontFamily="@font/font_lexend"
            android:background="@drawable/bg_pre"
        app:iconifiedByDefault="false"
        android:layout_marginBottom="10dp"/>

        <!-- Tổng số truyện -->
        <TextView
            android:id="@+id/tvTotalStoriesCount"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tổng số truyện: 0"
            android:fontFamily="@font/font_lexend"
            android:textSize="16sp"
            android:textColor="@color/black"
            android:layout_marginBottom="10dp"/>

        <!-- ProgressBar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"/>

        <!-- RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewStories"
            android:layout_width="match_parent"
            android:fontFamily="@font/font_lexend"
            android:layout_height="0dp"
            android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="100dp"
        android:scrollbars="vertical"/>

    </LinearLayout>

    <!-- Nút thêm truyện (FloatingActionButton) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_story"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:contentDescription="Thêm truyện"
        app:backgroundTint="@color/white"
        app:srcCompat="@drawable/ic_writes"
        android:layout_marginBottom="90dp"
        android:layout_marginEnd="16dp" />

</FrameLayout>
